# Version configuration - edit this file for releases
# Single source of truth for app versioning

# MARKETING_VERSION (CFBundleShortVersionString)
# What users see in About dialog and App Store
# Format: MAJOR.MINOR.PATCH (semantic versioning)
MARKETING_VERSION="0.1.0"

# BUILD_NUMBER (CFBundleVersion)
# What Sparkle compares for updates - must always increase
# Format: Simple integer, incremented with each build
BUILD_NUMBER="1"

# Bundle ID - unique identifier for the app
# Used by: Keychain, Launch Services, code signing, permissions
# WARNING: Changing this after release breaks user settings and updates
BUNDLE_ID="com.chrisedwards.optimusclip"
